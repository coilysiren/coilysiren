# Per-repo command allowlist for coilysiren (profile README repo). No dev
# verbs - present so coily's repocfg loader recognizes the repo.

commands: {}

# Catalog metadata for the cross-repo knowledge graph. Schema: relaxed
# in agentic-os v0.19.0 (description + dependsOn required; kind / type /
# system / owner / lifecycle deprecated, pending fleet strip). Tracker:
# https://forgejo.coilysiren.me/coilyco-bridge/agentic-os-kai/issues/643
catalog:
  kind: Component
  type: documentation
  system: content
  owner: kai
  lifecycle: production
  description: "GitHub profile README. Landing page for the lights-out factory."
  dependsOn: []
  providesApis: []
