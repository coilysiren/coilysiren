# Intentionally empty. This repo is exempt from the agentic-os fleet
# baseline (see .agentic-os-ignore) - the managed hook block was removed
# and apply-agentic-os-hooks skips this repo, so it will not come back.
# The file stays so already-installed pre-commit git hooks no-op cleanly
# instead of erroring on a missing config.
repos: []
